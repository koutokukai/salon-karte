# サロンカルテ製造所 — 開発規約

このリポジトリは「サロンカルテ製造所」プロジェクトの実装です。
セッションを跨いでも **6人のスタッフ体制と役割分担は絶対にブレさせないこと**。

## 必読
- `docs/00-ORG.md` … 組織構成・役割・承認ライン（**変更にはユーザー承認が必要**）
- `docs/SPEC.md` … プロジェクト仕様書 v1.0（原本）
- `docs/70-target-strategy.md` … ターゲット・価格・営業方針 v2（**最新。50 の初案を上書き**）
- `docs/80-edison-staff-design.md` … スタッフ管理・操作ログ・一括管理の設計
- `docs/60-gen-status.md` … 判断記録（採用／保留／却下の理由）

## 絶対ルール（事業）
1. ターゲットは**駆け出し・自宅サロン・1店舗個人事業主**。大型店は狙わない。
2. **恒久無料プランは作らない。3ヶ月無料トライアルに統一。**
3. 課金単位は人数ではなく**アカウント数**（個人1=980円／店舗2込=2,980円／追加1=+500円）。
4. **店舗ごとにコードを分岐させない。** 個別要望は設定（独自項目・表示切替・プリセット）で吸収する。
5. **ボタン配置を店ごとに変えない。** 誤操作防止が UI 設計の根幹。

## 絶対ルール（技術）
1. `tenants.salon_type` は**登録時に1度だけ**設定する。以降の UPDATE に含めない。
   バリデーション層（`src/lib/validation.ts`）で必ず拒否すること。
2. カルテは**全業種の全カラムを1テーブル**で保持する。業種ごとにテーブルを割らない。
   該当しない業種のカラムは NULL のまま。
3. **予約管理機能は作らない**（サロンボードとの棲み分け）。提案も却下する。
4. すべてのクエリは `tenant_id` でスコープする。テナント間のデータ漏洩は最優先の防止対象。
5. UI 分岐は `KarteForm` が `tenant.salon_type` を読み、業種別子コンポーネントを呼ぶ形に統一。

## 役割の境界（越えない）
- ゲンさん = 統括・判断／リチャード = 調査・提案（実装NG）／サレオツ = UI設計（開発NG）
- メガネ = 拡張視点（暴走NG）／エジソン = 実装・技術判断（営業NG）／ひろぽん = 営業・自動化（開発NG）
